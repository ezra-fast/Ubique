// scheduled_job_persistence.h
// scheduled_job_persistence() is called before looping begins if persistence has not been established via other means

#pragma once

bool scheduled_job_persistence();

